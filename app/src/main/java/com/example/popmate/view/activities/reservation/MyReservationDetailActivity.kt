package com.example.popmate.view.activities.reservation

import android.os.Bundle
import com.example.popmate.R
import com.example.popmate.config.BaseActivity
import com.example.popmate.databinding.ActivityPopupDetailBinding

class MyReservationDetailActivity :
    BaseActivity<ActivityPopupDetailBinding>(R.layout.activity_my_reservation_detail) {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
    }
}
